@section SampleHeading{<span class="sampleName"> Grid/FrozenColumns</span>
}
@section ControlsSection{
    @(Html.EJ().Grid<EditableOrder>("Scrolling")
        .Datasource((IEnumerable<object>)ViewBag.datasource)
        .AllowScrolling()
		.AllowPaging()
        .ScrollSettings(scroll => { scroll.Height(300).Width(760); })
        .Columns(col =>
        {
            col.Field("OrderID").HeaderText("Order ID").IsPrimaryKey(true).TextAlign(TextAlign.Right).Width(90).IsFrozen(true).Add();
            col.Field("CustomerID").HeaderText("Customer ID").Width(100).Add();
            col.Field("EmployeeID").HeaderText("Employee ID").TextAlign(TextAlign.Right).Width(100).IsFrozen(true).Add();
            col.Field("Freight").HeaderText("Freight").TextAlign(TextAlign.Right).Width(90).Format("{0:C}").Add();
            col.Field("ShipCity").HeaderText("Ship City").Width(110).Add();
            col.Field("ShipName").HeaderText("Ship Name").Width(170).IsFrozen(true).Add();
            col.Field("OrderDate").HeaderText("Order Date").Width(100).TextAlign(TextAlign.Right).Format("{0:MM/dd/yyyy}").Add();
            col.Field("ShipCountry").HeaderText("ShipCountry").Width(110).Add();
            col.Field("ShipPostalCode").HeaderText("Ship Postal Code").Width(140).Add();
            col.Field("Verified").HeaderText("Verified").Width(100).Add();
        })
        )
}
