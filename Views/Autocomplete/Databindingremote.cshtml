@section SampleHeading{
    <span class="sampleName">Autocomplete-Databinding - Remote Data-ASP.NET MVC-SYNCFUSION</span>}
@section MetaTags{
    <meta name="description" content="This example demonstrates how to bind remote data in the Syncfusion ASP.NET MVC Autocomplete component.">
}
@section ControlsSection{
    <div class="frame">
        <div class="control">
            @{
                Html.EJ()
                    .Autocomplete("searchCustomer")
                    .Datasource(d => d.URL("//js.syncfusion.com/ejServices/wcf/NorthWind.svc/").Offline(false).CrossDomain(true))
                    .Query("ej.Query().from('Suppliers').select('SupplierID', 'ContactName')")
                    .AutocompleteFields(f => f.Text("ContactName").Key("SupplierID"))
                    .Width("100%")
                    .WatermarkText("Search a customer")
                    .Render();
            }
        </div>
    </div>
}

